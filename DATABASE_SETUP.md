# Database Setup Moved

The active database documentation is now:

`docs/current/DATABASE_SETUP.md`

This root file is kept as a compatibility pointer for older prompts and handoff notes that still reference `DATABASE_SETUP.md`.

Read `PROJECT_CONTEXT.md` first, then use `docs/current/DATABASE_SETUP.md` for the current MySQL schema, alert evidence storage, validation logs, and dashboard views.
